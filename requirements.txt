# === Core Python & System ===
numpy==1.26.4
pandas==2.2.3
matplotlib==3.9.2
pillow==10.4.0

# === Image / Video Processing ===
opencv-python==4.10.0.84
pillow-heif==0.15.0           # Enables reading HEIC/HEIF images (iPhone photos)

# === Machine Learning / Deep Learning ===
tensorflow==2.17.0             # Includes Keras (stable for MobileNetV2, GPU/CPU compatible)
scikit-learn==1.5.2            # For class weights, metrics, etc.

# === Data Augmentation ===
albumentations==1.4.18
imgaug==0.4.0

# === Progress & Utilities ===
tqdm==4.66.5                   # Progress bars for preprocessing & training
loguru==0.7.2                  # Pretty logging (optional, nice for debugging)

# === Notebook & Interactive Tools ===
jupyter==1.1.1                 # Core Jupyter Notebook support
ipykernel==6.29.5              # Allows VS Code to detect the environment as a Jupyter kernel
notebook==7.2.2                # Notebook server
jupyterlab==4.2.5              # (Optional) Better notebook UI in browser
ipywidgets==8.1.3              # For interactive plots/widgets in notebooks

# === Visualization (optional but useful) ===
seaborn==0.13.2
plotly==5.24.1

# === Image I/O & Metadata (optional) ===
imageio==2.36.0
exifread==3.0.0

# === Optional Backend for Model Export ===
onnx==1.17.0                   # For model conversion (optional)
onnxruntime==1.19.2            # For inference testing (optional)

# === System / Misc ===
requests==2.32.3
python-dotenv==1.0.1
